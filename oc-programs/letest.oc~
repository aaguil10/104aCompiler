mytaco(jumbo, ball(jelly), boy(), que);

struct hand {
  int fingers;
}

struct dude {
   int age;
   bool sex;
   void run;
}

void[] f() {};

void kill(int gun, char bubble){
  if( pollo == chiken ){
     cook[time.five];
  }else{
    return "Que?";
    while( i > 5 ){
      int goo = 5;
      string cow = "moo";
    }
  }
}
